@using AwesomeCare.Admin.ViewModels.Client;
@model List<CreateClientCareObj>
@{
    ViewData["Title"] = "Reports";
}

<div class="row clearfix">
    <div class="col-lg-12 col-md-12">
        <div class="card planned_task">
            <div class="card-header">
                <h2>All Care Objectives</h2>
            </div>
            <div class="card-body">
                <table class="table m-b-0 datatables">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Client</th>
                            <th>Date</th>
                            <th>Status</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Select((d, i) => new { index = i + 1, data = d }))
                        {
                            <tr>
                                <td>@item.index </td>
                                <td>@item.data.ClientName</td>
                                <td>@item.data.Date</td>
                                <td>@item.data.StatusName</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <button id="btnGroupDrop1" type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                            @Html.ActionLink("Edit", "Edit", "ClientCareObj", new { CareObjId = item.data.CareObjId }, new { @class = "dropdown-item" })
                                            @Html.ActionLink("View", "View", "ClientCareObj", new { CareObjId = item.data.CareObjId }, new { @class = "dropdown-item" })
                                            @Html.ActionLink("Download", "Download", "ClientCareObj", new { CareObjId = item.data.CareObjId }, new { @class = "dropdown-item" })
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
