@using AwesomeCare.DataTransferObject.DTOs.BaseRecord;
@model List<GetBaseRecordWithItems>

@{
    ViewData["Title"] = "Base Record";
}

<h2>Base Record</h2>


<div class="row clearfix">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <h2>Base Record Items</h2>
            </div>
            <div class="card-body">
                <div class="accordion" id="accordion">
                    @foreach (var itemDesc in Model.Select(x => x.Description).Distinct())
                    {
                        var Desc = itemDesc.Replace(" ", string.Empty);
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0 row">
                                    <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#@Desc" aria-expanded="false" aria-controls="@Desc">
                                        @Desc
                                    </button>
                                </h5>
                            </div>
                            <div id="@Desc" class="collapse" aria-labelledby="@Desc" data-parent="#accordion">

                                @foreach (var baseRecord in Model)
                                {
                                    if (Desc == baseRecord.Description)
                                    {
                                        <div class="card-body">
                                            <div class="card">
                                                <div class="card-header">
                                                    <h5 class="mb-0 row">
                                                        <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#@baseRecord.BaseRecordId" aria-expanded="false" aria-controls="@baseRecord.BaseRecordId">
                                                            <small>(Key: @baseRecord.KeyName)</small>
                                                        </button>
                                                        <a asp-action="AddBaseRecordItem" asp-controller="Admin" asp-route-baseRecord="@baseRecord.KeyName" asp-route-baseRecordId="@baseRecord.BaseRecordId" class="col-sm-offset-1 col-sm-1" title="Add"><span class="sr-only">Add</span> <i class="fa fa-plus"></i></a>
                                                    </h5>
                                                </div>
                                                <div id="@baseRecord.BaseRecordId" class="collapse" aria-labelledby="@baseRecord.KeyName" data-parent="@string.Concat("#",Desc)">
                                                    <div class="card-body">
                                                        <table class="table table-condensed table-borderless table-hover">
                                                            @foreach (var item in baseRecord.BaseRecordItems)
                                                            {
                                                                //BaseRecord Content
                                                                <tr>
                                                                    <td> @item.ValueName</td>
                                                                    <td>   <a asp-controller="Admin" asp-action="EditBaseRecordItem" asp-route-baseRecordItemId="@item.BaseRecordItemId"> <i class="fa fa-pencil m-r-5" /></a></td>
                                                                </tr>

                                                            }
                                                        </table>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                }
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row clearfix">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <h2>Base Record PIN</h2>
            </div>
            <div class="card-body">
                <div class="accordion" id="pin">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0 row">
                                <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#rottering" aria-expanded="false" aria-controls="rottering">
                                    Rottering
                                </button>
                            </h5>
                        </div>
                        <div id="rottering" class="collapse" aria-labelledby="rottering" data-parent="#pin">
                            <div class="card-body">
                                <div class="card">
                                    <div class="card-header">
                                        <h5 class="mb-0 row">
                                            <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#rotaId" aria-expanded="false" aria-controls="rotaId">
                                                <small>(Key: Rottering PIN)</small>
                                            </button>
                                        </h5>
                                    </div>
                                    <div id="rotaId" class="collapse" aria-labelledby="RotteringPIN" data-parent="#rottering">
                                        <div class="card-body">
                                            <table class="table table-condensed table-borderless table-hover">
                                                <tr>
                                                    <td> ********</td>
                                                    <td>
                                                        <button type="button" class="btn btn-primary fa fa-pencil m-r-5" data-bs-toggle="modal" data-bs-target="#roteringModal">
                                                        </button>
                                                    </td>
                                                </tr>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
                <div class="accordion" id="taskpin">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0 row">
                                <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#taskboard" aria-expanded="false" aria-controls="taskboard">
                                    Task Board
                                </button>
                            </h5>
                        </div>
                        <div id="taskboard" class="collapse" aria-labelledby="taskboard" data-parent="#taskpin">
                            <div class="card-body">
                                <div class="card">
                                    <div class="card-header">
                                        <h5 class="mb-0 row">
                                            <button class="btn btn-link col-sm-10" type="button" data-toggle="collapse" data-target="#taskId" aria-expanded="false" aria-controls="taskId">
                                                <small>(Key: Task Board PIN)</small>
                                            </button>
                                        </h5>
                                    </div>
                                    <div id="taskId" class="collapse" aria-labelledby="TaskboardPIN" data-parent="#taskboard">
                                        <div class="card-body">
                                            <table class="table table-condensed table-borderless table-hover">
                                                <tr>
                                                    <td> ********</td>
                                                    <td>
                                                        <button type="button" class="btn btn-primary fa fa-pencil m-r-5" data-bs-toggle="modal" data-bs-target="#taskModal">
                                                        </button>
                                                    </td>
                                                </tr>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="roteringModal" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form id="RoteringForm" asp-controller="Rotering" asp-action="ChangePin" method="post">
                <div class="modal-body">
                    <div class="card">

                        <div class="card-header">
                        </div>
                        <div class="card-body">
                            <div class="form-group">
                                <label>Please Enter Old Pin</label>
                                <input type="password" class="form-control" name="oldPin" />
                            </div>
                            <div class="form-group">
                                <label>Please Enter New pin</label>
                                <input type="password" class="form-control" name="newPin" />
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <input type="submit" class="btn btn-primary" value="Submit" />
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </form>
        </div>
    </div>
</div>
<div class="modal fade" id="taskModal" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form id="TaskBoardForm" asp-controller="TaskBoard" asp-action="ChangePin" method="post">
                <div class="modal-body">
                    <div class="card">

                        <div class="card-header">
                        </div>
                        <div class="card-body">
                            <div class="form-group">
                                <label>Please Enter Old Pin</label>
                                <input type="password" class="form-control" name="oldPin" />
                            </div>
                            <div class="form-group">
                                <label>Please Enter New pin</label>
                                <input type="password" class="form-control" name="newPin" />
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <input type="submit" class="btn btn-primary" value="Submit" />
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </form>
        </div>
    </div>
</div>
@section VendorScripts{
    <script src="~/assets2/js/jquery-3.6.0.min.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('[id^="btnRotering"]').on('click', function () {
                const Btn = $(this);
                const Id = Btn.data('id');
                $('#clientId').val(Id);
            });
            $('[id^="#btnRoteringGrid"]').on('click', function () {
                const Btn = $(this);
                const Id = Btn.data('id');
                console.log(Id);
                $('#clientId').val(Id);
            });
            $('#RoteringForm').submit(function () {
                const id = $('#clientId').val();
                const pin = $('#pin').val();
                console.log(pin);
                if (pin != undefined) {
                    shownotification("Please enter your pin code", "error");
                    return false;
                }
                else {
                    if (id > 0) {
                        return true;
                    }


                }
            });
        });
    </script>
}