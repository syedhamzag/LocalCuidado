@using AwesomeCare.Admin.ViewModels.Client;
@model List<CreateClientEyeHealthMonitoring>
@{
    ViewData["Title"] = "Reports";
}

<div class="row clearfix">
    <div class="col-lg-12 col-md-12">
        <div class="card planned_task">
            <div class="header">
                <h2>All Client Eye Health Monitoring</h2>
            </div>
            <div class="body">
                <table class="table m-b-0 datatables">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Client</th>
                            <th>Date</th>
                            <th>Due Date</th>
                            <th>Status</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Select((d, i) => new { index = i + 1, data = d }))
                        {
                            <tr>
                                <td>@item.index </td>
                                <td>@item.data.ClientName</td>
                                <td>@item.data.Date</td>
                                <td>@item.data.Deadline</td>
                                <td>@item.data.StatusName</td>
                                <td>
                                    @if (item.data.MethodUsedAttach != "No Image")
                                    {
                                        <a href="#" class="on-default showfile-btn" title="Download" data-id="@item.data.MethodUsedAttach" style="margin-left:5px;"><i class="fa fa-file"></i></a>
                                    }
                                    @if (item.data.ToolUsedAttach != "No Image")
                                    {
                                        <a href="#" class="on-default showfile-btn" title="Download" data-id="@item.data.ToolUsedAttach" style="margin-left:5px;"><i class="fa fa-file"></i></a>
                                    }

                                    @if (item.data.StatusAttach != "No Image")
                                    {
                                        <a href="#" class="on-default showfile-btn" title="Download" data-id="@item.data.StatusAttach" style="margin-left:5px;"><i class="fa fa-file"></i></a>
                                    }

                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <button id="btnGroupDrop1" type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                            @Html.ActionLink("Edit", "Edit", "ClientEyeHealthMonitoring", new { EyeHealthId = item.data.EyeHealthId }, new { @class = "dropdown-item" })
                                            @Html.ActionLink("View", "View", "ClientEyeHealthMonitoring", new { eyeId = item.data.EyeHealthId }, new { @class = "dropdown-item" })
                                            <button type="button" class="dropdown-item" id="@item.data.EyeHealthId" data-toggle="modal" data-target="#EmailModal" onclick="Setid(this)">
                                                Email
                                            </button>
                                            @Html.ActionLink("Download", "Download", "ClientEyeHealthMonitoring", new { eyeId = item.data.EyeHealthId }, new { @class = "dropdown-item" })
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="modal" id="EmailModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form action="Email" method="post">
                <div class="modal-body">
                    <div class="row">
                        <select class="form-group col-sm-12 col-xs-12 col-md-12 col-lg-12" name="Smtp">
                            <option value="smtp.gmail.com">Gmail</option>
                            <option value="smtp.live.com">Hotmail</option>
                            <option value="smtp.mail.yahoo.com">Yahoo</option>
                        </select>
                        <input type="hidden" id="EyeHealthId" name="eyeId" />
                        <div class="form-group col-sm-12 col-xs-12 col-md-12 col-lg-12">
                            <label class="control-label">Your Email</label>
                            <input type="text" name="sender" class="form-control" />
                        </div>
                        <div class="form-group col-sm-12 col-xs-12 col-md-12 col-lg-12">
                            <label class="control-label">Your Password</label>
                            <input type="password" name="password" class="form-control" />
                        </div>
                        <div class="form-group col-sm-12 col-xs-12 col-md-12 col-lg-12">
                            <label class="control-label">Email Sent To</label>
                            <input type="text" name="recipient" class="form-control" />
                        </div>
                    </div>
                    <Label></Label>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-primary">Send Email</button>
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section VendorScripts{
    <script src="../../../assets/vendor/jquery-steps/jquery.steps.js"></script> <!-- JQuery Steps Plugin Js -->
    <script src="../../../assets/vendor/dropify/js/dropify.min.js"></script>
    <script src="../../../assets/bundles/mainscripts.bundle.js"></script>
}

@section Scripts{
    <script src="../../../assets/js/pages/forms/form-wizard.js"></script>
    <script src="../../../assets/js/pages/forms/dropify.js"></script>
    <script>
        $('.datatables').on('click', '.showfile-btn', function () {
            const showBtn = $(this);
            const Id = showBtn.data('id');
            window.open(Id);
        });
        function Setid(e) {
            var Id = e.id;
            $('#EyeHealthId').val(Id);
        }
    </script>
}

