@using AwesomeCare.DataTransferObject.DTOs.ClientComplainRegister;
@model List<GetClientComplainRegister>

@{
    ViewData["Title"] = "Client - Complain Register ";
}

<div class="row clearfix">
    <div class="col-lg-12 col-md-12">
        <div class="card planned_task">
            <div class="header">
                <h2>All Complaints</h2>
            </div>
            <div class="body">
                <table class="table m-b-0 datatables">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Link</th>
                            <th>Due Date</th>
                            <th>Remarks</th>

                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Select((d, i) => new { index = i + 1, data = d }))
                        {
                        <tr>
                            <td>@item.index </td>
                            <td>@item.data.LINK</td>
                            <td>@item.data.DUEDATE</td>
                            <td>@item.data.REMARK</td>

                            <td>
                                <div class="btn-group" role="group">
                                    <button id="btnGroupDrop1" type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    </button>
                                    <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                        @Html.ActionLink("Edit", "Edit", "Complain", new { id = item.data.ComplainId }, new { @class = "dropdown-item" })
                                    </div>
                                </div>
                            </td>
                        </tr>
                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

